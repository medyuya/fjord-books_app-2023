<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('views.books.edit.link_text'), edit_book_path(@book) %> |
  <%= link_to t('views.books.index.link_text'), books_path %>

  <%= button_to t('views.books.destroy.btn_text'), @book, method: :delete %>
</div>
